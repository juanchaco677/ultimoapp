export class Util {
    constructor(
      
      ){
    
      }

    formatearFecha(fecha:Date) {
        
        return fecha.getFullYear()+"/"+fecha.getMonth()+"/"+fecha.getDate()+"";
    }
  
}
  